import React, {useRef} from 'react';
import {View, Text} from 'react-native';

const LetterG = ({top = 0, left = 0}) => {
  return (
    <View>
      <Text>Letter Segmentation</Text>
    </View>
  );
};

export default LetterG;
